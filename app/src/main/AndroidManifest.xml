<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xtec">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="CarpoolingTEC"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".pcarros"></activity>
        <activity android:name=".addNewCar"/>
        <activity android:name=".Carro"/>
        <activity android:name=".pcanjear" />
        <activity android:name=".peliminarCuenta" />
        <activity android:name=".peditaarInfo" />
        <activity android:name=".peditar" />
        <activity android:name=".pagregarCarro" />
        <activity android:name=".p_preguntaCarro" />
        <activity android:name=".pregistarse" />
        <activity android:name=".pantalla_principal" />
        <activity android:name=".pvalidarDATIC" /> <!-- <activity android:name=".pingresar" /> -->
        <activity android:name=".MainActivity"/>
        <activity
            android:name=".pingresar"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>